package com.gallery_app.core_service.mq.dto;

import lombok.EqualsAndHashCode;
import lombok.ToString;

@EqualsAndHashCode(callSuper = true) // Includes fields from the superclass in equals/hashCode
@ToString(callSuper = true)
public class FromThumbnailGenerationServiceDTO extends InboundDTO {
    //
}
